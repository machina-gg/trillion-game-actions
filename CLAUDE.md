# trillion-game-actions — このリポジトリの規約

複数のリポジトリで共有する **CI 資産**（レビュー観点・Approve を押す reusable workflow・呼び出し元の CI に差し込む composite action）を置くリポジトリ。
呼び方・プロファイル一覧・判定行の契約は [`README.md`](README.md) が SSOT。

## public リポジトリである

**秘密・内部事情を書かない。** ここに置くファイルは誰でも読める。

- トークン・API キー・個人のローカルパス（`/Users/...` 等）・個人のメールアドレスを書かない
- 非公開リポジトリの名前・構成・運用の内情を書かない。観点は**技術スタックの性質**で書く
- レビュー観点は「どのリポジトリの話か」ではなく「何を見るか」で書く

## 観点と Approve スクリプトの SSOT はここ

- レビュー観点の実体は `perspectives/` にある。他のリポジトリは**コピーせずリンクで参照する**
- 判定行の契約（`## レビュー(reviewer)` の見出しと行頭 `判定:` 行）は
  `perspectives/common.md` と `scripts/approve-if-verdict.sh` の**双方が守る**。
  ⚠ **片方だけを変えない**——形式を変えると判定が読まれず Approve が付かなくなる。
  変えるときは `tests/approve-if-verdict.test.sh` を同じ PR で更新する
- `scripts/approve-if-verdict.sh` は**決定的処理**（同じ入力なら同じ出力）に徹する。
  判断は Claude 側（`perspectives/`）に置き、スクリプトへ持ち込まない

## 変更のしかた

- 変更は Issue → ブランチ → PR → レビュー → マージの順で進める（Issue は運営リポジトリ側に置く）
- 呼び出し元の CI の中で走るもの（`.github/workflows/` / `.github/actions/` / `scripts/`）の変更は、**レビュアーの判定を人間が確認したうえで人間がマージする**（改竄されると呼び出し元すべてのレビューと Approve に影響するため）
- `perspectives/` と `README.md` の変更も、**呼び出し元すべてに即時反映される**（`@main` 参照）。
  1 リポジトリの都合で共通の観点を曲げない。特定のスタックの話はプロファイルに分ける
- 機能追加・変更時は `README.md` を同時に更新する（プロファイルを増やしたら一覧に載せる）

## 書き方

- コードには日本語コメントを書く。**その場で知らないと間違える情報だけ**を書き、
  設計の経緯・検討した代替案は書かない（コメントは腐る）
- **腐るものを書かない。** 日付・実測値・件数を残さず、必要なら測り方（コマンド）を書く
- **同じ内容を 2 箇所に書かない。** 実体は 1 箇所に置き、他はリンクで参照する
- fail-open と fail-close を意識して選び、選んだ側をコメントに明記する。
  Approve に関わる判定は**押さない側**（fail-close）へ倒す

## 検査

PR ごとに CI（job 名 `check`）が走る。手元で実行するコマンドは [`README.md`](README.md)「ローカルでの検査」が SSOT（ここに再掲しない）。
